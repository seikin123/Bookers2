<h1>User info#index</h1>
<%= render 'users/profile' %>

<%= render 'books/form' %>

</thead>
<h2>Users</h2>
<td>image</td>
<td>name</td>
<table>
<% @users.each do |user| %>
<td><%= attachment_image_tag user, :profile_image, fallback: "no_image.jpg" %></td>
<td><%= user.name %></td>
<td><%= link_to "show", user_path(user.id) %></td>
<% end %>
</table>

